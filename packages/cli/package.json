{
	"name": "@sundayceo/create",
	"version": "0.0.0",
	"type": "module",
	"license": "MIT",
	"bin": {
		"create-sundayceo": "dist/index.js"
	},
	"publishConfig": {
		"access": "public"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsup && node --import tsx/esm scripts/copy-templates.ts",
		"check-types": "tsc --noEmit",
		"dev": "tsup --watch",
		"test": "vitest run"
	},
	"dependencies": {
		"@clack/prompts": "^0.11.0"
	},
	"devDependencies": {
		"@sundayceo/typescript-config": "workspace:*",
		"@types/node": "catalog:",
		"tsup": "catalog:",
		"tsx": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
